spring:
  application:
    name: ass1
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/2024_Summer_Coding_Test?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: =dl-13579

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
